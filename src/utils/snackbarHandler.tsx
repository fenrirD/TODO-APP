import {useContext} from "react";
import {CustomSnackbarContext} from "../components/CustomSnackbar/CustomSnackbarContext";

export const SnackbarHandler = (err:any) => {


  // console.log(err,snackbarContext)

  const errFun = () => {
    // const snackbarContext =
  }

  return {
    errFun
  }
}